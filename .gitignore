# Secrets and local env — never commit a filled .env (copy from .env.example only)
.env
.env.*
!.env.example
# Cookie/session files (do not commit)
cookie.txt
cookies.txt
*.cookie

# Python
__pycache__/
.venv/
venv/

# Scraper inputs/outputs (local)
scraped_outputs/
urls.txt
